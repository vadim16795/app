apiVersion: apps/v1
kind: Deployment
metadata:
  name: stage-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stage-app
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: stage-app
    spec:
      containers:
      - name: stage-app
        image: image_name
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"